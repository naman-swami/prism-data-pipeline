annotations:
  cost: none
  read_only: true
  requires_confirmation: false
description: Trace column-level data transformations across DAG dependencies.
input_schema:
  additionalProperties: false
  properties:
    dag_id:
      type: string
    table_name:
      type: string
  required:
  - table_name
  - dag_id
  type: object
name: validate-data-lineage
output_schema:
  additionalProperties: false
  properties:
    confidence:
      type: number
    data:
      type: string
    status:
      type: string
  required:
  - status
  - data
  type: object
